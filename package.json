{
  "name": "pg-management-system",
  "version": "1.0.0",
  "description": "Root workspace of PG Management System",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client --legacy-peer-deps",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "npx concurrently -k -n \"backend,frontend\" -c \"purple,blue\" \"npm run dev:server\" \"npm run dev:client\""
  }
}
